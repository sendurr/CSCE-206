from math import sin, cos, pi
x=(pi)//4
y=(sin(x))**2 + (cos(x))**2
print (y)

#sin^2(x)+cos^2(x) does equal 1, needed to import Pi from math, 1_val was not a valid syntax, and that was the improper method in which to square a value.
