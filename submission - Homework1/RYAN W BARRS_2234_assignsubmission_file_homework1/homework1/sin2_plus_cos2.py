from math import sin, cos, pi


# x = pi/4
# 1_val = sin^2(x) + cos^2(x)
# print 1_val

#must import pi
#1_val should be val_1
#** must be used instead of ^ to square sin and cos; also wrong order
x = pi/4
val_1 = sin(x)**2 + cos(x)**2
print val_1
#So yes, (a) is equal to 1