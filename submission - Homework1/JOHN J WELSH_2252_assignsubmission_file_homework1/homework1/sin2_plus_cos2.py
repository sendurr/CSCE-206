#Homework 1, Exercise 2 by John Welsh

from math import sin, cos, pi 		#Need to import pi as well as sin and cos
x = pi/4
val_1 = sin(x)**2 + cos(x)**2		#Variable cannot start with number, 
									#(x) must come before power,
									#Power operator is ** not ^
print val_1							#Variable in print statement must be correct