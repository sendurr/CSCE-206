from math import sin, cos, pi 
x = pi/4
Value1= sin(x)**2 + cos(x)**2
print (Value1)

#Need to import pi as well as insert "**" for "^" and move that behind the sin(x) and cos(x)
#also need to fix the variable because the 1_val, the 1 was purple.