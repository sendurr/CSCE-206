import math
from math import sin, cos
#brings in needed packages

x = math.pi / 4
#defines x

answer = sin (x) * sin(x) + cos (x) * cos(x)
# computes answer sinx*sinx = sin^2(x)

print ("The answer to sin^(pi/4) + cos^2(pi/4) is:")
print answer
#prints answer, finishes code